name: daily-update

on:
  schedule:
    # 一天兩班。腳本用台北當地日期推算交易日(taipei_today()),不是 runner 的
    # UTC 日期, 所以排程時間怎麼移動都不會抓錯交易日。

    # 【第一班】台北 14:30(收盤後) = UTC 06:30 —— 算出「當日」的位階
    # 期交所日盤 13:45 收盤。probe-taifex 實測當日 OI 在台北 14:39 就已發布
    # (14:39 / 14:49 兩次量到都齊全; 07:18 那次則是「有列但無日盤資料」)。
    # 另外 GitHub 排程本來就不準時。實測本 repo 的 35 次排程執行, 誤點中位數
    # 48 分、平均 71 分、最長 175 分(近三小時), 且同一個時段的變異極大 ——
    # 曾用過的 08:00 UTC 時段有時 3 分鐘就跑、有時拖到 175 分, 大概是整點
    # 又逢熱門時段所致(23:00 UTC 那班只誤點 13~18 分)。
    # 這對本用途無害: 我們只怕太早(OI 還沒發布), 不怕太晚, 誤點反而更安全。
    # 萬一真的早跑而 OI 還沒出來也無妨: prepare() 只留日盤, 當天沒有日盤列就
    # 不會被算進去, 這班等於空轉, 不會寫出錯誤資料。
    - cron: "30 6 * * 1-5"

    # 【第二班】台北 07:00(隔日開盤前) = UTC 23:00 —— 補上盤前快照
    # 此時當日夜盤(前日 15:00~當日 05:00)已收, 期交所已發布夜盤收盤價, 但當日
    # 日盤還沒開、沒有 OI。這班用夜盤價重算 gex_now/regime, 產生 preopen_*。
    # UTC 週一 23:00 = 台北週二 07:00, 涵蓋週一那個交易日; UTC 週五 23:00 =
    # 台北週六 07:00, 涵蓋週五。所以 UTC 的 1-5 剛好對應交易日週一到週五。
    - cron: "0 23 * * 1-5"
  workflow_dispatch: {}   # 允許手動觸發

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pandas requests numpy scipy

      - name: Run stock pipeline
        continue-on-error: true
        run: python scripts/update_data.py

      - name: Run GEX pipeline
        continue-on-error: true
        run: python scripts/txo_gex.py

      # 美股靠 Yahoo 的非官方 API, 被擋的機率比前兩條高, 一樣不能拖累其他人
      - name: Run US breadth pipeline
        continue-on-error: true
        run: python scripts/us_breadth.py

      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/theme_overrides.csv data/history.csv.gz docs/data/latest.json data/gex_history.csv docs/data/gex_latest.json data/shares.csv data/breadth.csv data/us_breadth.csv docs/data/us_latest.json
          git diff --cached --quiet || git commit -m "data: $(date +%F)"
          git push
